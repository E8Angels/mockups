# Changelog — Sponsor CRM

- 2026-05-16 — jordan — Migrated from e8-portal docs/ to folder-per-feature layout in E8Angels/mockups.
- 2026-05-21 — jordan — Fix broken cross-page links: drop `sponsor-crm-` prefix on hrefs (files already live in `sponsor-crm/`).
- 2026-05-21 — jordan — Major iteration: single-page sponsor detail (no tabs); collapsible/resizable left filter rail on staff + board dashboards; New Sponsor and Edit Sponsor dialogs; e8.logger email auto-ingest into the Activity timeline; TipTap rich-text notes; explicit follow-ups (no per-note "next step"); priority field removed; calmed stage/type palette; full names + email addresses throughout; plan rewritten to drop historical narrative.
- 2026-05-21 — jordan — Fix JS parse error: Lee O'Connor apostrophe terminated single-quoted strings in STAFF arrays; switched those literals to double quotes so the dashboard data renders.
- 2026-05-21 — jordan — Kanban tile redesign: replaced the cramped multi-row card with a four-band layout (gray header band carrying type + entity tag(s) + owner initials; bold sponsor name; lighter contact line supporting comma-separated multi-contact; full-dollar ask amount; separator + follow-up footer with overdue/amber/slate states). Entity tag label shortened from `c3`/`c6` to `(c)3`/`(c)6`; the `both` entity now renders both tags instead of a `both` label. Tile applied to staff Kanban and board read-only Kanban; plan §6a/§6b.iv updated.
- 2026-05-21 — jordan — Drop the `N — ` numeric prefix from every stage label (chips, column headers, filter rows, list view, activity timeline, impact-report pipeline table, New Sponsor select). Drop the per-column sponsor count from Kanban column headers (staff keeps the ask total; board header is now clean). Fix the activity-timeline rail so the colored dots sit centered on the line (`left:-25.5px` to compensate for the ol's 2px border + 20px padding) instead of beside it.
